---
title: 截屏翻译软件 - 方案设计 v2
date: 2026-07-20
tags: [project, design, windows, rust, slint, ocr, llm]
category: project
status: draft-v2
---

# 🖼️ 截屏翻译软件 - 方案设计 v2

> Windows 桌面截屏翻译，类有道截屏翻译体验。
> 技术栈：Rust + Slint；本地 OCR + 大模型翻译；整块行级覆盖；常驻托盘 + 全局热键。

## v2 修订说明（相对 v1）

1. **修复分发包遗漏 `onnxruntime.dll`**（ort 动态链接，必须随包分发），重新核算体积预算；
2. **重写 OCR 流程**：det/cls/rec 三件套，删除"单字 bbox 合并成行"的错误设计，rec 输出真实置信度；
3. **新增结果窗口 ResultWindow 设计**（v1 完全没有结果展示与交互环节）；
4. 目标指标**增加端到端延迟指标**；解决"空闲内存 ≤30MB"与"OCR 启动预热"的矛盾（改为懒加载）；
5. 行对齐从"自然语言约束行数"改为**编号标记 + 校验重试**，删除不可行的"等分填回"兜底；
6. **API Key 改用 DPAPI 加密存储**；配置/历史移入 `%APPDATA%`；安装器不再强制写自启动；
7. 覆盖渲染增强：复杂背景降级策略、颜色量化众数、字号迭代收缩（从 v0.4 提前到 v0.1）；
8. Overlay 改为**透明窗口 + 压暗挖洞**（v1 的整体 opacity 会连选区一起压暗）；DPI 换算提前到 v0.1；
9. 新增**隐私、测试策略、开源合规**三章；
10. 依赖版本对齐实现期最新稳定版：Slint 1.17（原生托盘，替代 tray-icon crate）、ort 2.x。

---

## 一、目标指标

| 维度 | 目标 | 备注 |
|---|---|---|
| 平台 | Windows 10/11（1809+），x64 | |
| 分发包体积（zip） | ≤ 30 MB | 含 onnxruntime.dll + OCR 模型 + 兜底字体 |
| 解压后占用 | ≤ 45 MB | |
| 空闲常驻内存 | ≤ 40 MB（挑战目标 ≤ 25 MB） | **OCR 懒加载**：未触发翻译前不加载模型；不缓存全屏帧 |
| 翻译峰值内存 | ≤ 250 MB | 含 OCR session + 全屏图像缓冲 |
| 冷启动到托盘就绪 | ≤ 1 s | OCR 模型不在启动路径上 |
| **选区确认 → 首行译文上屏** | **P90 ≤ 3 s** | 核心体验指标：OCR ≤ 1 s + LLM 首 token ≤ 1.5 s |
| 选区确认 → 全部译文完成 | P90 ≤ 6 s | 以 200 字以内选区为基准 |

> v1 的"启动时间 ≤500ms"与"冷启动到首帧 ≤1s"语义重叠，合并为"冷启动到托盘就绪"。

> **内存测量口径**：以任务管理器"专用工作集"（Private Working Set）为准，应用静置后台 5 分钟后读数。
> 注意 Windows 会对后台进程做工作集修剪，任务管理器默认列显示的"内存"（工作集）会被人为压低
> （如有道翻译显示 ~10 MB），对标时应统一用"专用工作集/提交大小"口径，避免跨口径比较。
> 该指标的目的是防止失控（全屏位图、OCR 模型、截图缓存意外常驻），而非对标原生 C++ 应用的极限优化水位。

---

## 二、技术栈

| 模块 | 选型 | 体积 |
|---|---|---|
| 语言 | Rust（stable，以实现期为准） | - |
| UI | Slint 1.17（声明式 `.slint`，编译期检查） | ~3 MB |
| 渲染 | Skia（Slint 自带） | ~5 MB |
| 系统托盘 | **Slint 1.17 原生系统托盘**（备选 `tray-icon` crate） | 0 / <1 MB |
| 截屏 | `windows` crate + DXGI Desktop Duplication，GDI 兜底 | <1 MB |
| 全局热键 | `windows` crate `RegisterHotKey` | <1 MB |
| ONNX 推理 | `ort` 2.x（onnxruntime Rust 绑定） | <1 MB |
| **ONNX Runtime 运行时** | **`onnxruntime.dll`（CPU 版，随包分发）** | **~11 MB** |
| OCR 模型 | RapidOCR-ONNX PP-OCRv4：**det 3.1 + cls 1.4 + rec 10 + 字典 2** | ~16.5 MB |
| HTTP / SSE | `reqwest` + `eventsource-stream` | ~3 MB |
| SQLite | `rusqlite`（bundled 静态链接） | ~2 MB |
| 剪贴板 | `arboard` crate | <1 MB |
| 密钥保护 | Windows DPAPI（`CryptProtectData`） | 0（系统 API） |
| 单实例 / 自启动 / 图像 / 字体 | `single-instance` / `winreg` / `image` / `ab_glyph` | ~2 MB |
| **合计（解压后）** | | **~45 MB** |

> zip  deflate 压缩后约 25–28 MB，满足 ≤30 MB 预算。exe 本体约 8–12 MB。

---

## 三、架构

```
┌────────────────────────────────────────────────────────┐
│  ScreenTranslator (Rust binary)                        │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │  UI Layer (Slint 1.17)                           │   │
│  │  ├─ SettingsWindow   主设置面板                  │   │
│  │  ├─ OverlayWindow    截屏选区（全屏透明+挖洞）   │   │
│  │  ├─ ResultWindow     翻译结果（钉住/复制/保存）  │   │
│  │  ├─ StatusToast      状态浮窗（OCR中/翻译中）    │   │
│  │  └─ TrayIcon         原生系统托盘                │   │
│  └────────────────────┬────────────────────────────┘   │
│                       │ 事件/回调                       │
│  ┌────────────────────▼────────────────────────────┐   │
│  │  App Core (Rust)                                 │   │
│  │  ├─ AppState       全局状态（Arc<RwLock<>>）     │   │
│  │  ├─ HotkeyManager  全局热键监听                  │   │
│  │  ├─ TrayManager    托盘菜单                      │   │
│  │  └─ Pipeline       截屏→OCR→对齐翻译→渲染→展示   │   │
│  │       └─ CancelToken  随时取消（ESC/再次热键）   │   │
│  └──────┬────────────┬────────────┬─────────────┘   │
│         │            │            │                     │
│    ┌────▼────┐  ┌────▼────┐  ┌────▼────┐               │
│    │ Capture │  │  OCR    │  │  LLM    │               │
│    │ windows │  │  ort2.x │  │ reqwest │               │
│    │ + DXGI  │  │det/cls/ │  │  + SSE  │               │
│    │物理像素 │  │  rec    │  │编号对齐 │               │
│    └────┬────┘  └────┬────┘  └────┬────┘               │
│         │            │            │                     │
│    ┌────▼────────────▼────────────▼─────┐              │
│    │  Renderer (ab_glyph + image)       │              │
│    │  背景采样 + 擦除/降级底条 + 绘制    │              │
│    └────────────────┬───────────────────┘              │
│                     ▼                                   │
│              ResultWindow 展示                           │
│                                                         │
│  ┌─────────────────────────────────────────────┐       │
│  │  Storage (%APPDATA%\ScreenTranslator\)       │       │
│  │  ├─ Config    settings.toml（Key 密文）      │       │
│  │  ├─ KeyVault  DPAPI 加解密                   │       │
│  │  └─ History   history.db（SQLite）           │       │
│  └─────────────────────────────────────────────┘       │
└────────────────────────────────────────────────────────┘
```

---

## 四、关键实现

### 4.1 截屏

**方案**：`windows` crate + Direct3D 11 + DXGI Desktop Duplication
- **输出物理像素**；同时记录显示器 `scale_factor`，与 Slint 逻辑坐标互转（选区坐标 → 截图像素坐标必须换算，否则 150% 缩放机器上选区与截图错位）。
- v0.1 仅支持主屏；v0.3 扩展多屏（在鼠标所在屏创建 Overlay）。
- 部分独占全屏应用（少数游戏）截不到 → 兜底 GDI（`PrintWindow` / `BitBlt`）。

### 4.2 选区覆盖层

**不用窗口整体 opacity**（会连选区一起压暗），改为：完全透明窗口 + 四块半透明矩形在选区周围围出"洞"，选区内画面保持清晰：

```slint
// ui/overlay.slint
export component OverlayWindow inherits Window {
    // 实际尺寸 = 屏幕逻辑尺寸，创建时设置
    background: transparent;
    always-on-top: true;

    // 选区（逻辑坐标），由 Rust 侧拖拽时更新
    in-out property <length> sel-x: 0px;
    in-out property <length> sel-y: 0px;
    in-out property <length> sel-w: 0px;
    in-out property <length> sel-h: 0px;

    // 四块压暗矩形围出选区"洞"
    Rectangle { x: 0; y: 0; width: parent.width; height: sel-y;            background: #00000080; }
    Rectangle { x: 0; y: sel-y + sel-h; width: parent.width;
                height: parent.height - sel-y - sel-h;                     background: #00000080; }
    Rectangle { x: 0; y: sel-y; width: sel-x; height: sel-h;               background: #00000080; }
    Rectangle { x: sel-x + sel-w; y: sel-y; height: sel-h;
                width: parent.width - sel-x - sel-w;                       background: #00000080; }

    // 选区边框 + 尺寸标签
    Rectangle {
        x: sel-x; y: sel-y; width: sel-w; height: sel-h;
        border-color: #0af; border-width: 2px;
    }

    // 鼠标交互：Slint 的 Window 没有 on_mouse_move，用 TouchArea
    TouchArea {
        moved => { /* 更新 sel-x/y/w/h */ }
        pressed => { /* 记录起点 */ }
    }

    Text {
        text: "拖动鼠标框选区域，ESC / 右键取消";
        color: white;
        y: parent.height - 40px;
    }
}
```

- ESC / 右键取消：Overlay 打开时注册局部热键，窗口需先抢焦点。
- 选区确认后：逻辑坐标 × scale_factor → 物理像素坐标，从 DXGI 帧中裁剪。

### 4.3 OCR

**模型**（RapidOCR-ONNX，PP-OCRv4 三件套）：
- `ch_PP-OCRv4_det_infer.onnx`（文本检测，~3.1 MB）
- `ch_PP-OCRv4_cls_infer.onnx`（方向分类，~1.4 MB）
- `ch_PP-OCRv4_rec_infer.onnx`（文本识别，~10 MB）
- `ppocr_keys_v4.txt`（中英文字典，~2 MB）

**重要澄清**：PP-OCR 的 det（DB 算法）输出**本身就是行级文本框**，rec 对整行裁剪图**直接输出行级字符串**。不存在"单字 bbox"，v1 的"单字合并成行"是错误设计，已删除。

**加载策略**：懒加载——首次翻译时才加载三个 session（约 500 ms，配合"识别中…"toast）；设置里提供"后台预热"开关，默认关闭（保住空闲内存指标）。

**输出**：`Vec<TextBlock { text, polygon: Vec<Point>, confidence: f32 }>`，polygon 为行级多边形，confidence 为 rec 各 token 概率均值（供"低置信度多模态兜底"使用，**不得再硬编码 0.0**）。

```rust
use ort::session::Session;

pub struct OcrEngine {
    det: Session,
    cls: Session,
    rec: Session,
    keys: Vec<String>,
}

impl OcrEngine {
    pub fn recognize(&mut self, img: &DynamicImage) -> Result<Vec<TextBlock>> {
        // 1. det 预处理：保持长宽比缩放，长边 ≤960，归一化，NCHW
        let det_in = preprocess_det(img, 960);

        // 2. det 推理 + DB 后处理 → 行级多边形（物理像素坐标）
        let polys = db_postprocess(self.det.run(ort::inputs![det_in]?)?);

        // 3. 按多边形最小外接矩形透视矫正裁剪
        let mut crops: Vec<(Polygon, DynamicImage)> =
            polys.iter().map(|p| (p.clone(), warp_crop(img, p))).collect();

        // 4. cls 方向分类：0°/180°，必要时旋转
        self.correct_orientation(&mut crops)?;

        // 5. rec 批量推理（高 48px、宽按长宽比、batch=8）
        //    同时累计 token 概率 → 行置信度
        let results = self.rec_batch(&crops)?;

        // 6. 阅读顺序排序：按 (y 聚类成行带, 行带内按 x) 排序
        //    多栏版面先做列聚类再排序
        Ok(sort_reading_order(polys, results))
    }
}
```

**已知限制**（写入 troubleshooting 文档）：竖排文字（漫画、古籍）、艺术字/描边字识别率差 → 引导用户开启多模态兜底。

### 4.4 翻译（编号对齐 + 流式 SSE）

**协议**：OpenAI 兼容（覆盖 GPT / DeepSeek / 通义 / 智谱 / Ollama）。

**行对齐策略**（替换 v1 的"保持相同行数"自然语言约束）：
给每行加编号标记，要求模型保留标记输出；解析按标记对齐，缺失时重试，再失败才降级。

```rust
use reqwest::Client;
use futures::stream::StreamExt;
use eventsource_stream::Eventsource;

pub async fn translate_stream(
    lines: &[String],
    target_lang: &str,
    config: &LlmConfig,
    cancel: CancellationToken,
    mut on_line: impl FnMut(usize, String) + Send,
) -> Result<Vec<String>> {
    // 1. 编号标记：<1>原文\n<2>原文…  要求输出保留编号
    let numbered: String = lines.iter().enumerate()
        .map(|(i, l)| format!("<{}>{}", i + 1, l))
        .collect::<Vec<_>>().join("\n");

    let system = "你是翻译引擎。将用户给的带编号文本逐条翻译为目标语言。\
                  必须保留每条的 <N> 编号，一条对应一条，\
                  不合并、不拆分、不解释、不输出任何其他内容。";
    let user = format!("目标语言：{}\n\n{}", target_lang, numbered);

    let body = serde_json::json!({
        "model": config.model,
        "messages": [
            { "role": "system", "content": system },
            { "role": "user", "content": user }
        ],
        "stream": true,
        "temperature": 0.3
    });

    // 2. 请求：30s 超时，支持代理（系统代理 / 手动配置），可取消
    let resp = Client::builder()
        .timeout(std::time::Duration::from_secs(30))
        .proxy(config.proxy.clone())   // 国内访问 OpenAI 刚需
        .build()?
        .post(&config.api_base)
        .bearer_auth(config.decrypted_key()?)  // DPAPI 解密
        .json(&body)
        .send().await?;

    let mut stream = resp.bytes_stream().eventsource();
    let mut buf = String::new();

    loop {
        tokio::select! {
            _ = cancel.cancelled() => anyhow::bail!("cancelled"),
            event = stream.next() => {
                let Some(event) = event else { break };
                let data = event?.data;
                if data == "[DONE]" { break; }

                let delta = parse_delta(&data)?;
                // 3. 剥离推理模型的思考内容：
                //    reasoning_content 字段 / <think>...</think> 标签
                buf.push_str(&delta.content_only());

                // 4. 命中完整 <N>… 段即解析推给 UI（流式逐条上屏）
                while let Some((idx, text, rest)) = take_complete_segment(&buf) {
                    on_line(idx, text);
                    buf = rest;
                }
            }
        }
    }

    // 5. 完整性校验 + 兜底
    let aligned = reconcile(lines.len(), &buf)?;
    if aligned.missing.is_empty() { return Ok(aligned.lines); }

    // 缺行 → 严格 prompt 整体重试 1 次
    // 仍缺 → 对缺失行单独补翻（逐行请求，牺牲上下文换完整）
    fill_missing(lines, &aligned, config).await
}
```

**流式渲染的错位回滚**：流式阶段按到达顺序乐观上屏；全部收齐后做最终对齐校验，若发现错位，用最终结果**整体重绘一次**结果图（UI 上表现为一次闪烁，可接受）。

### 4.5 覆盖渲染

**整块覆盖流程**（修复 v1 的性能 bug + 增强健壮性）：

```rust
use image::{RgbaImage, Rgba};

fn render_overlay(
    original: &DynamicImage,
    text_blocks: &[TextBlock],
    translations: &HashMap<usize, String>,
) -> DynamicImage {
    let mut img = original.to_rgba8();          // 循环外只转一次（v1 在循环内重复拷贝）
    let src = original.to_rgba8();

    for (idx, block) in text_blocks.iter().enumerate() {
        let bbox = block.polygon.bounding_box();

        // 1. 背景采样：先 clamp 坐标，再按 4bit/通道量化后取众数
        let (bg, purity) = sample_background_color(&src, bbox);

        if purity >= 0.6 {
            // 纯色背景：直接填色擦除
            fill_rect(&mut img, bbox, bg);
        } else {
            // 复杂背景（渐变/图片）：降级为半透明圆角底条
            fill_rounded_rect(&mut img, bbox, Rgba([0, 0, 0, 180]));
        }

        // 2. 绘制译文
        if let Some(t) = translations.get(&idx) {
            // 字号：bbox 高 ×0.75 起步，迭代收缩 + bbox 内自动换行，
            // 最小 10px；仍放不下允许 bbox 向下扩展一行高
            let font_size = fit_font_size(t, bbox, 0.75, 10.0);
            // 字色：bbox 内非背景像素中位色，与背景对比度不足时兜底黑/白
            let color = pick_text_color(&src, bbox, bg, purity);
            draw_text_wrapped(&mut img, t, bbox, font_size, color);
        }
    }

    DynamicImage::ImageRgba8(img)
}
```

```rust
fn sample_background_color(img: &RgbaImage, bbox: Rect) -> (Rgba<u8>, f32) {
    let pad = 4;
    let mut hist = std::collections::HashMap::new::<u32, usize>::new();
    let mut total = 0usize;

    // 四条边采样；坐标先 clamp 到图像范围（v1 负坐标 as u32 会回绕）
    for (x, y) in border_points(bbox, pad, img.width(), img.height()) {
        let p = img.get_pixel(x, y);
        // 4bit/通道量化，抗照片/压缩噪声
        let key = ((p[0] >> 4) as u32) << 8 | ((p[1] >> 4) as u32) << 4 | (p[2] >> 4) as u32;
        *hist.entry(key).or_default() += 1;
        total += 1;
    }

    let (key, count) = hist.into_iter().max_by_key(|(_, c)| *c).unwrap();
    (unquantize(key), count as f32 / total as f32)   // 返回众数色 + 占比纯度
}
```

### 4.6 结果窗口 ResultWindow（v1 缺失，本章补全）

渲染合成的结果图需要一个承载窗口——这是用户每次使用的最终交互界面。

```
┌──────────────────────────────────────┐
│  [翻译结果图，可滚动]                  │
│                                       │
│  （流式：逐行淡入；对齐校验后整体校正） │
├──────────────────────────────────────┤
│ 📌 钉住  📋 复制译文  🖼 复制图       │
│ 💾 保存PNG  🕘 历史  ✕ 关闭(ESC)      │
└──────────────────────────────────────┘
```

- 位置：选区附近弹出，不遮挡选区；可拖动。
- **钉住**：always-on-top，滚轮缩放，类有道 pin 窗口。
- 复制译文：所有行 `\n` 拼接进剪贴板；复制图：PNG 进剪贴板。
- 流式期间窗口先显示原图 + 逐行覆盖；翻译中按 ESC / 再次热键 → `CancelToken` 取消请求。
- 错误态：OCR 无结果 / API 失败（欠费、超时、代理不通）在窗口内显示可操作的错误条（"重试 / 打开设置"），而非静默失败。

---

## 五、数据流与隐私

**默认链路（不发送图像）**：

```
截图 → 本地 OCR（离线） → 仅文字行 → LLM API → 译文 → 本地渲染
```

- 截图图像本身**不会**发送给任何云端接口；发给 LLM 的只有 OCR 识别出的**纯文本行**。
- **唯一例外**：用户显式开启"识别率低时自动用多模态模型"后，低置信度选区的**裁剪图**会发送给视觉模型（Qwen-VL / OpenAI vision）。首次开启时弹窗明示此行为，设置页常驻标注。
- 完全离线路径：本地 OCR + Ollama 本地模型，无任何数据出机。
- **API Key**：使用 Windows DPAPI（`CryptProtectData`）加密后存入 `settings.toml`，密文绑定当前用户，拷走配置文件也无法解密。
- **数据位置**：配置、历史全部存于 `%APPDATA%\ScreenTranslator\`（安装目录在 Program Files 下普通用户不可写）。
- **历史记录**：仅存原文/译文文本与可选缩略图；提供"不记录历史"开关；截图原图默认不落盘。

---

## 六、项目结构

```
screen-translator/
├── Cargo.toml
├── assets/
│   ├── ocr/
│   │   ├── ch_PP-OCRv4_det_infer.onnx
│   │   ├── ch_PP-OCRv4_cls_infer.onnx
│   │   ├── ch_PP-OCRv4_rec_infer.onnx
│   │   └── ppocr_keys_v4.txt
│   ├── fonts/
│   │   └── SourceHanSansSC-subset.otf   # 兜底字体 ~3MB（OFL）
│   └── icon.ico
├── ui/
│   ├── settings.slint
│   ├── overlay.slint
│   ├── result.slint          # 结果窗口（v2 新增）
│   └── toast.slint
├── src/
│   ├── main.rs
│   ├── app.rs                 # AppState + Pipeline + CancelToken
│   ├── config.rs              # TOML 读写（%APPDATA%）
│   ├── capture/{dxgi.rs, gdi.rs}
│   ├── ocr/{engine.rs, preprocess.rs, postprocess.rs, models.rs}
│   ├── translate/
│   │   ├── openai.rs          # OpenAI 兼容 + 编号对齐
│   │   ├── ollama.rs
│   │   ├── parser.rs          # SSE 解析 + 推理内容剥离 + 对齐校验
│   │   └── parser_tests.rs    # 解析器单测（v2 新增）
│   ├── render/{overlay.rs, color.rs, font.rs}
│   ├── platform/
│   │   ├── hotkey.rs
│   │   ├── tray.rs            # Slint 原生托盘
│   │   ├── clipboard.rs       # arboard（v2 新增）
│   │   ├── autostart.rs
│   │   └── single.rs
│   ├── security/
│   │   └── dpapi.rs           # Key 加解密（v2 新增）
│   ├── history/db.rs
│   └── ui_bridge/handlers.rs
├── installer/installer.nsi
├── tests/
│   ├── fixtures/              # OCR/渲染回归图集
│   └── golden/                # 渲染 golden image
└── docs/
    ├── api.md
    ├── privacy.md             # 隐私说明（v2 新增）
    ├── licenses.md            # 第三方许可（v2 新增）
    └── troubleshooting.md
```

---

## 七、Cargo.toml

```toml
[package]
name = "screen-translator"
version = "0.1.0"
edition = "2021"

[dependencies]
slint = "1.17"                                # 与技术栈表一致（v1 写成 1.7）
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync"] }  # 不用 full，省体积
reqwest = { version = "0.12", features = ["stream", "json", "socks"] }              # socks: 代理支持
eventsource-stream = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"                                  # v1 遗漏：配置读写
futures = "0.3"                               # v1 遗漏：示例代码用了 StreamExt
ort = { version = "2", features = ["download-binaries"] }  # ort 2.x；分发时随包附带 onnxruntime.dll
image = "0.25"
rusqlite = { version = "0.31", features = ["bundled"] }
arboard = "3"                                 # v1 遗漏：剪贴板（设置面板有两个复制功能）
winreg = "0.52"
single-instance = "0.4"
anyhow = "1"
thiserror = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
ab_glyph = "0.2"
directories = "5"

[target.'cfg(windows)'.dependencies]
windows = { version = "0.62", features = [    # 版本以实现期最新稳定为准
    "Win32_Foundation",
    "Win32_Graphics_Direct3D11",
    "Win32_Graphics_Dxgi",
    "Win32_Graphics_Gdi",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_Security_Cryptography",            # DPAPI
    "Win32_UI_HiDpi",                         # DPI awareness
] }

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
strip = true
panic = "abort"
```

> 所有第三方 crate 版本号在实现启动时以 crates.io 最新稳定版刷新一次，并锁定 `Cargo.lock`。

---

## 八、设置面板

```
┌────────────────────────────────────────────┐
│  ⚙ 截屏翻译设置                              │
├────────────────────────────────────────────┤
│  快捷键:    [Ctrl + Alt + T]      [录制]   │
│  ☐ 开机自启                                │
│  ☐ 截屏后复制原文到剪贴板                   │
│  ☐ 翻译后复制译文到剪贴板                   │
│  ☐ 不保留历史记录                           │
│                                            │
│  ── 翻译 ──                                 │
│  引擎:    [OpenAI 兼容       ▼]            │
│  API 地址: [https://api.openai.com/v1]      │
│  API Key:  [············]  (DPAPI 加密存储) │
│  模型:     [gpt-4o-mini           ▼]       │
│  目标语言: [简体中文               ▼]       │
│  代理:    [系统代理 ▼]  [主机:端口______]   │
│  预设: [OpenAI] [DeepSeek] [通义] [智谱]    │
│        [Ollama 本地] [自定义]               │
│                                            │
│  ── OCR ──                                  │
│  引擎: [本地 RapidOCR (离线可用) ▼]         │
│  ☐ 启动时后台预热模型（占内存换速度）        │
│  ☐ 识别率低时自动用多模态模型 (Qwen-VL 等) │
│    ⚠ 开启后低置信度选区图片会发送给云端      │
│                                            │
│  ── 显示 ──                                 │
│  译文字体: [Microsoft YaHei UI]             │
│  译文颜色: [智能取色 ▼]                     │
│                                            │
│  ── 历史 ──  [查看]  [清空]  [导出]         │
│  ── 关于 ──  [隐私说明]  [第三方许可]       │
│                                            │
│  [测试连接]              [保存]   [取消]    │
└────────────────────────────────────────────┘
```

---

## 九、打包发布

### 9.1 分发包结构（补齐 onnxruntime.dll）

```
ScreenTranslator-v0.1.0-win64.zip  (~25-28 MB)
├── ScreenTranslator.exe
├── onnxruntime.dll                # v1 遗漏，ort 动态链接必需
├── assets/
│   ├── ocr/ (det/cls/rec/keys)
│   └── fonts/SourceHanSansSC-subset.otf
├── README.md
├── licenses.md
└── LICENSE
```

CI 构建时校验：`ScreenTranslator.exe` 的导入表必须能解析到随包的 `onnxruntime.dll`（防止漏发）。

### 9.2 NSIS 安装脚本（修正 v1 三处问题）

```nsi
Name "Screen Translator"
OutFile "ScreenTranslator-Setup.exe"
InstallDir "$PROGRAMFILES\ScreenTranslator"
RequestExecutionLevel admin

Section "Main"
  SetOutPath $INSTDIR
  File "ScreenTranslator.exe"      ; v1 误用 File /r（/r 是递归目录）
  File "onnxruntime.dll"           ; v1 遗漏
  SetOutPath $INSTDIR\assets
  File /r "assets\*"
  WriteUninstaller $INSTDIR\uninstall.exe
SectionEnd

; 自启动改为可选，默认不勾选（v1 无条件写注册表，与应用内开关冲突）
Section /o "开机自启"
  WriteRegStr HKCU "Software\Microsoft\Windows\CurrentVersion\Run" \
    "ScreenTranslator" "$INSTDIR\ScreenTranslator.exe"
SectionEnd
```

配置文件不放安装目录——首次运行时由应用在 `%APPDATA%\ScreenTranslator\` 创建。

---

## 十、测试策略（v2 新增）

| 层 | 内容 |
|---|---|
| 单元测试 | SSE 解析器：跨 chunk 分片、`[DONE]`、reasoning 剥离；编号对齐：缺号 / 多行 / 乱序 / 空行；颜色量化众数；字号收缩收敛 |
| 契约测试 | mock LLM server 回放各家（OpenAI/DeepSeek/通义）真实 SSE 报文 |
| 回归测试 | OCR fixture 图集（网页/游戏/代码/表格截图）：识别准确率 + 耗时基准 |
| 渲染测试 | golden image 对比（固定字体 + fixture 截图，容差阈值） |
| 集成测试 | CI（windows-latest）：release 构建 + 冒烟（加载模型→识别 fixture→mock 翻译→出图） |
| 手动验收 | 150%/200% DPI、双屏、暗色/亮色网页、独占全屏游戏 |

---

## 十一、开源合规（v2 新增，v1.0 公开分发前置项）

| 组件 | 许可 | 义务 |
|---|---|---|
| Slint | GPLv3 / Royalty-free / 商业 三选一 | 选 **Royalty-free**：桌面应用免费，但须在关于页和文档署名 "Made with Slint"；不愿署名则需 GPL 开源或购买商业许可 |
| RapidOCR 模型 | Apache-2.0 | 保留 LICENSE/NOTICE |
| ONNX Runtime | MIT | 保留许可文本 |
| 思源黑体（兜底字体） | SIL OFL | 可再分发，不得单独售卖 |
| 微软雅黑等系统字体 | 不可再分发 | 只能运行时调用系统已装字体，**不得打包** |

关于页列出全部第三方组件及许可链接；`docs/licenses.md` 随包分发。

---

## 十二、迭代路线图

### v0.1 MVP（聚焦"能用且不出错"）
- [ ] Cargo 骨架 + Slint 1.17 窗口
- [ ] 主屏 DXGI 截屏 + **DPI 物理/逻辑坐标换算**（从 v0.3 提前）
- [ ] 透明 Overlay（挖洞压暗）+ TouchArea 拖拽 + ESC 取消
- [ ] OCR 三件套（det/cls/rec）懒加载 + 真实置信度 + 阅读顺序排序
- [ ] OpenAI 兼容同步翻译 + **编号对齐 + 校验重试**
- [ ] 覆盖渲染：填色擦除 + 复杂背景降级底条 + **字号迭代收缩/自动换行**（从 v0.4 提前）
- [ ] **ResultWindow**（展示/复制译文/保存 PNG/关闭）
- [ ] 基础设置（API Key/模型/代理/目标语言）

### v0.2（体验完整化）
- [ ] 流式翻译（边收边渲染 + 错位回滚）
- [ ] Slint 原生托盘 + 托盘菜单
- [ ] 全局热键 + 单实例（二次启动唤醒设置面板）
- [ ] SQLite 历史 + DPAPI 密钥存储
- [ ] 剪贴板功能（原文/译文/图）
- [ ] 设置面板完整化

### v0.3（场景扩展）
- [ ] 开机自启（应用内开关）
- [ ] 多模态 fallback（OpenAI vision / Qwen-VL，含首次开启隐私弹窗）
- [ ] Ollama 本地引擎
- [ ] 多屏支持
- [ ] 托盘图标动态状态

### v0.4（打磨）
- [ ] 竖排文字识别评估
- [ ] 复杂背景 inpainting 替代底条
- [ ] NSIS 安装包 + 卸载
- [ ] 中英文 UI

### v1.0（发布）
- [ ] 按字 bbox 精修覆盖
- [ ] 自动更新（GitHub Releases）
- [ ] **开源合规审查**（Slint 署名、licenses.md）
- [ ] 公开分发

---

## 十三、风险

| 风险 | 影响 | 应对 |
|---|---|---|
| onnxruntime.dll 漏分发 → 用户机器无法启动 | 高 | 随包分发 + CI 导入表校验 |
| Slint 1.17 原生托盘 API 不稳定 | 中 | 抽象 TrayManager，回退 `tray-icon` crate |
| OCR 首次加载 ~500ms 卡体验 | 低 | 懒加载 + toast 提示；可选预热开关（默认关） |
| 复杂背景下填色擦除难看 | 高 | 纯度检测降级半透明底条；v0.4 inpainting |
| 推理模型（R1/o 系）输出思考内容 | 中 | parser 剥离 reasoning；预设默认推荐非推理模型 |
| LLM 行对齐失败 | 中 | 编号标记 + 严格重试 + 缺行单独补翻 |
| 中文字体在精简系统缺失 | 低 | 打包思源黑体 subset（OFL，~3MB） |
| DXGI 截不到独占全屏 | 低 | 文档说明，提示窗口模式 |
| 各家 SSE 字段差异 | 中 | mock server 契约测试覆盖主流 provider |
| 高 DPI 坐标错位 | 高 | v0.1 即做物理/逻辑换算，手动验收覆盖 150%/200% |
