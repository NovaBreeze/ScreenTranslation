# 隐私说明

- 截图和 OCR 均在本机处理，原始截图默认不写入磁盘。
- 云端翻译请求只包含 OCR 得到的文字行、目标语言和必要的编号标记。
- API Key 使用 Windows DPAPI 加密，密文仅能由当前 Windows 用户解密。
- 启用历史记录时，仅保存原文和译文，可在 `%APPDATA%\ScreenTranslator\history.db` 中找到。
- 只有在用户显式开启“低置信度多模态兜底”并确认隐私提示后，低置信度文字行的裁剪图才会发送到所配置的视觉模型；完整截图不会上传。
